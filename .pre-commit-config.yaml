repos:
  - repo: local
    hooks:
      # - id: ruff-lint
      #   name: ruff lint (fix)
      #   entry: poetry run ruff check --fix
      #   language: system
      #   types: [python]

      - id: ruff-format
        name: ruff format
        entry: uv run ruff format
        language: system
        types: [python]
      
      # - id: test
      #   name: pytest
      #   entry: uv run pytest -v
      #   language: system
      #   types: [python]

